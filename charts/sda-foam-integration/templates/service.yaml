apiVersion: v1
kind: Service
metadata:
  name: {{ include "sda-foam-integration.fullname" . }}
  labels:
    {{- include "sda-foam-integration.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sda-foam-integration.selectorLabels" . | nindent 4 }}
