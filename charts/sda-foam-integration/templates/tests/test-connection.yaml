apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sda-foam-integration.fullname" . }}-test-connection"
  labels:
    {{- include "sda-foam-integration.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sda-foam-integration.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
